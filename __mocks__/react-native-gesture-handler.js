// Minimal stub for react-native-gesture-handler.
module.exports = {
  Gesture: {
    Pan: () => ({}),
    Exclusive: () => ({}),
  },
  GestureDetector: ({ children }) => children,
  GestureHandlerRootView: ({ children }) => children,
};
